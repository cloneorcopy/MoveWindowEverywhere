# WorkBuddy 开发任务：Move Window Everywhere

请在以下目录中直接完成开发：

`E:\EX_library\code\Move window everywhere`

## 一、项目目标

开发一个 Windows 桌面工具，暂定名称为 **Move Window Everywhere**。

目标工作流：

1. 软件启动后不显示主窗口，常驻 Windows 系统托盘并在后台运行。
2. 用户按下全局快捷键。
3. 软件立即记录快捷键触发瞬间鼠标所在的显示器。
4. 弹出窗口选择器，显示当前打开的应用程序窗口列表。
5. 用户通过鼠标双击或键盘 Enter 选择一个窗口。
6. 被选窗口移动到快捷键触发时鼠标所在的显示器。
7. 选择器关闭，目标窗口被激活并显示在目标显示器上。

注意：这里的“鼠标所在位置”默认指**鼠标所在的显示器**，不是把窗口左上角移动到鼠标坐标。窗口默认应移动到目标显示器的工作区中央，同时保留原窗口大小；如果原窗口大于目标工作区，则将尺寸限制在工作区内。

## 二、技术约束

请使用方案 B：

- C#
- .NET 8 或当前机器已安装的兼容 .NET LTS 版本
- WPF
- Windows 原生 API，通过 P/Invoke 调用
- 默认面向 Windows 10/11
- 项目应支持 x64 发布
- 不要使用需要额外后台服务的架构
- 尽量不引入第三方 NuGet 包；托盘功能优先使用 `System.Windows.Forms.NotifyIcon`
- 不要复制 Hart365/move-to-monitor、AltTabber 或其他项目的代码；可以参考公开 API 和设计，但实现代码应自行编写
- 除构建所必需的依赖外，不要擅自安装软件或修改系统级配置

建议的解决方案结构：

```text
Move window everywhere/
├─ MoveWindowEverywhere.sln
├─ src/
│  └─ MoveWindowEverywhere/
│     ├─ MoveWindowEverywhere.csproj
│     ├─ App.xaml
│     ├─ App.xaml.cs
│     ├─ Native/
│     ├─ Models/
│     ├─ Services/
│     ├─ ViewModels/
│     ├─ Views/
│     └─ Resources/
├─ tests/
│  └─ MoveWindowEverywhere.Tests/
├─ README.md
└─ .gitignore
```

如果你认为目录结构需要调整，可以调整，但必须保持项目容易编译、维护和扩展。

## 三、核心功能要求

### 1. 托盘和生命周期

- 启动后不显示普通主窗口。
- 在系统托盘显示图标。
- 托盘右键菜单至少包含：
  - 打开窗口选择器
  - 设置快捷键
  - 关于
  - 退出
- 使用命名 Mutex 实现单实例运行。
- 重复启动时，不创建第二个实例，而是尝试唤起已有实例或显示提示。
- 退出时正确释放全局快捷键、托盘图标、隐藏窗口和 Mutex。
- 不要默认开机启动；可以预留设置项，但除非实现完整且经过验证，不要自动写入启动项。

### 2. 全局快捷键

- 使用 `RegisterHotKey` 和隐藏消息窗口接收 `WM_HOTKEY`。
- 默认快捷键使用 `Ctrl + Alt + M`。
- 快捷键必须可以修改并持久化。
- 如果快捷键被其他程序占用，应给出明确提示，并允许用户重新设置，而不是静默失败。
- 快捷键触发时先执行以下操作：
  1. `GetCursorPos` 获取鼠标位置；
  2. `MonitorFromPoint` 获取目标显示器；
  3. `GetMonitorInfo` 获取目标显示器的 `rcWork` 工作区；
  4. 保存这些信息；
  5. 然后再显示窗口选择器。
- 后续用户点击选择器时，不要重新根据鼠标位置计算目标显示器。目标显示器必须是快捷键触发瞬间捕获的显示器，避免用户点击选择器后鼠标位置发生变化导致目标错误。

### 3. 窗口枚举

使用 `EnumWindows` 枚举顶层窗口，并为每个窗口保存至少以下信息：

- `HWND`
- 窗口标题
- 进程 ID
- 进程名称或可执行文件名
- 窗口类名
- 当前窗口状态：普通、最大化、最小化
- 当前所在显示器（能可靠获取时）
- 可选：窗口图标

默认过滤规则：

- 排除软件自身的窗口。
- 排除不可见窗口。
- 排除没有有效标题的窗口。
- 排除 `WS_EX_TOOLWINDOW` 工具窗口。
- 排除有 owner 的普通从属窗口，避免大量弹窗和内部窗口污染列表。
- 排除被 DWM cloaking 的窗口。
- 排除任务栏、桌面、Program Manager 等系统窗口。
- 默认保留最小化窗口，因为用户可能希望把最小化的应用恢复到目标屏幕。
- 对无法读取进程名的窗口不要让整个枚举失败，显示 `未知进程` 即可。
- 窗口列表刷新时要处理窗口已经关闭的情况。

过滤规则请集中放在独立服务或策略类中，不要散落在 UI 代码里。

### 4. 窗口选择器

使用 WPF 实现一个简洁的选择器窗口。

至少包含：

- 搜索框。
- 窗口列表。
- 窗口标题。
- 进程名称。
- 可选的窗口图标。
- 当前目标显示器编号或名称提示。
- 空列表提示。
- 键盘导航。

交互要求：

- 快捷键打开后自动聚焦搜索框或列表。
- 输入文字可以按窗口标题、进程名筛选。
- Up/Down 选择项目。
- Enter 确认并移动选中窗口。
- 鼠标双击确认并移动选中窗口。
- Esc 取消并关闭选择器，不移动任何窗口。
- 选择器关闭后，目标窗口应成为前台窗口。
- 选择器应显示在捕获到的目标显示器工作区内，不要跑到其他显示器或屏幕外。
- 选择器不应被加入待移动窗口列表。
- 选择器打开后窗口列表应重新枚举一次，以尽量反映当前状态。

不要在 MVP 阶段为了实现缩略图引入复杂依赖。如果缩略图实现会显著增加复杂度，可以先只显示图标、标题、进程名和 PID，并在 README 中记录缩略图为后续增强项。

### 5. 移动窗口

使用窗口句柄，不要依赖窗口标题定位窗口。

目标显示器使用快捷键触发时保存的显示器信息。

移动逻辑：

1. 再次确认 `HWND` 仍然存在。
2. 读取当前窗口状态。
3. 如果窗口最小化，先恢复。
4. 如果窗口最大化，先恢复为普通窗口。
5. 读取恢复后的窗口尺寸。
6. 将窗口移动到目标显示器工作区中央。
7. 如果窗口尺寸大于工作区，限制宽度和高度，避免窗口跑出屏幕。
8. 如果需要，使用边界限制确保窗口完全位于目标工作区内。
9. 如果原状态是最大化，在目标显示器上重新最大化。
10. 如果原状态是最小化，默认恢复为可见普通窗口；如实现中选择继续最小化，必须在 README 中明确说明。
11. 激活目标窗口。
12. 对失败情况显示简洁、可理解的错误消息。

需要正确处理：

- 负坐标显示器。
- 左右排列和上下排列的显示器。
- 不同分辨率。
- 不同 DPI 缩放比例。
- 不同任务栏位置。
- 无边框窗口。
- 窗口已关闭或句柄失效。
- 目标程序以管理员权限运行。
- 某些程序拒绝外部移动的情况。

请在应用清单中声明 Per-Monitor V2 DPI 感知，并避免使用只适用于主显示器的 `A_ScreenWidth` 类似逻辑。

不需要强行绕过 Windows 的权限保护。对于权限不足的窗口，给出明确提示，例如“目标窗口权限高于本程序，请尝试以管理员身份运行本工具”。

### 6. 设置和持久化

至少持久化：

- 全局快捷键。
- 是否随 Windows 启动（如果实现）。

配置文件放在：

`%LOCALAPPDATA%\MoveWindowEverywhere\settings.json`

不要把配置写到项目目录，也不要把运行日志写到系统根目录。

### 7. 日志和错误处理

- 不需要联网、遥测或账户系统。
- 对关键失败记录简洁日志到 `%LOCALAPPDATA%\MoveWindowEverywhere\logs\`。
- 日志中不要记录敏感数据。
- P/Invoke 失败时保留 Win32 错误码，便于诊断。
- 不要用空的 catch 隐藏错误。
- UI 错误提示不能导致托盘程序退出。

## 四、测试和验证要求

不要只创建代码或项目骨架，必须实际完成构建和验证。

至少执行：

```text
dotnet --info
dotnet restore
dotnet build -c Release
dotnet test -c Release
```

如果环境中没有可用的 .NET SDK，先检查已有 SDK，不要未经必要性判断就安装软件。若确实无法构建，必须报告真实错误，不要伪造成功。

至少编写以下可测试逻辑的单元测试：

- 窗口过滤规则。
- 搜索筛选规则。
- 目标工作区内的窗口位置计算。
- 窗口尺寸大于目标工作区时的限制逻辑。
- 负坐标显示器位置计算。
- 目标窗口已关闭时的错误路径。

无法在无多显示器环境中自动验证的部分，编写清晰的手动测试清单，包括：

- 两台不同分辨率显示器。
- 一台位于主屏左侧或上方的显示器。
- 不同 DPI 缩放比例。
- 普通窗口、最大化窗口、最小化窗口。
- 管理员窗口，例如任务管理器。
- 快捷键冲突。
- 目标窗口在选择过程中被关闭。
- 取消选择。

## 五、交付物

完成后必须交付：

1. 可编译的完整源码。
2. Visual Studio 可打开的 `.sln` 文件。
3. 单元测试项目。
4. README.md，包含：
   - 功能说明；
   - 系统要求；
   - 开发环境；
   - 编译命令；
   - 发布命令；
   - 使用方法；
   - 快捷键修改方法；
   - 窗口过滤规则；
   - 管理员权限限制；
   - 已知限制；
   - 手动测试清单。
5. Release 构建产物，优先使用 self-contained、single-file、win-x64 发布方式；如果该方式导致明显问题，再说明原因并提供普通发布方式。
6. 构建、测试和发布命令的真实输出摘要。
7. 最终报告中明确列出：
   - 实际完成的功能；
   - 已验证的功能；
   - 未验证或存在限制的功能；
   - 生成的文件路径；
   - 构建和测试退出码。

## 六、开发纪律

- 先检查当前目录和开发环境，再创建项目。
- 只在 `E:\EX_library\code\Move window everywhere` 内创建和修改本项目文件。
- 不要删除或覆盖该目录外的文件。
- 不要停留在方案说明或伪代码，必须实现最小可运行版本。
- 先实现核心闭环：托盘 → 快捷键 → 枚举窗口 → 选择 → 移动 → 激活；再做图标、缩略图和美化。
- 不要因为缩略图、自动启动等非核心功能阻塞 MVP。
- 不要复制许可证不明确的第三方项目代码。
- 若遇到不影响核心功能的设计选择，采用合理默认值继续开发，并在 README 中记录，不要反复等待确认。
- 不要虚构未实际执行的构建、测试、运行或发布结果。

请现在开始：先检查环境和目录，然后创建项目、实现功能、编译、测试，并在最后给出真实的交付报告。
